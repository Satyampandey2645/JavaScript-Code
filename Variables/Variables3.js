// ----- var -----
console.log(a);  // undefined
var a = 67;

// ----- let -----
// console.log(b); // it will throw an error
let b = 100; 

// ----- const -----
const c = 90;
console.log(c);  // 90

// c = 100;   // it will throw an error because c is constant so that we can't change its value
// console.log(c);
// let c = 80; // it will throw an error because c is declared as a constant
